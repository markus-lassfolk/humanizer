# Swedish data pipeline (no --with-lm, no Wikipedia extended). Uploads logs
# and reports; opens an issue when any matrix leg fails.
name: Nightly pipelines

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  pipeline:
    name: pipeline (sv)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: package.json

      - run: npm ci

      - name: Swedish pipeline
        run: npm run sv:pipeline

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nightly-sv-reports
          retention-days: 14
          if-no-files-found: ignore
          path: |
            reports/
            locales/sv-se/references/PIPELINE-SNAPSHOT.md
            locales/sv-se/.pipeline/last-run.log

  notify:
    name: Open issue on failure
    needs: [pipeline]
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[nightly] Pipeline failed (${new Date().toISOString().slice(0, 10)})`,
              body: [
                'The scheduled **Nightly pipelines** workflow failed.',
                '',
                `- Workflow run: ${runUrl}`,
                '- Download artifact `nightly-sv-reports` from the run for logs and snapshots.',
                '',
                'Close this issue after fixing or if the failure was transient.',
              ].join('\n'),
            });
