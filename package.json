{
  "name": "humanizer",
  "version": "0.1.0",
  "description": "OpenClaw AI Humanizer - detect and remove AI writing patterns",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"TODO: add tests\" && exit 0",
    "lint": "echo \"TODO: add linter\" && exit 0",
    "build": "echo \"TODO: add build\" && exit 0"
  },
  "private": true
}
